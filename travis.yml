sudo: required
services:
  - docker

before_install:
  - docker build -t wallinsonlima/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true USERNAME/react-test npm test

after_success:
  - docker build -t wallinsonlima/multi-client ./client
  - docker build -t wallinsonlima/multi-nginx ./nginx
  - docker build -t wallinsonlima/multi-server ./server
  - docker build -t wallinsonlima/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push wallinsonlima/multi-client
  - docker push wallinsonlima/multi-nginx
  - docker push wallinsonlima/multi-server
  - docker push wallinsonlima/multi-worker